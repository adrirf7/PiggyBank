// This screen is never rendered – the tab button is overridden to open the
// add-transaction modal. Expo Router requires a file to exist for every tab.
export default function AddButtonPlaceholder() {
  return null;
}
